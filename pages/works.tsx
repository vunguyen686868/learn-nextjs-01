import { MainLayout } from '@/components/layout';
import * as React from 'react';

export interface WorksPagesProps {
}

export default function WorksPages(props: WorksPagesProps) {
    return (
        <div>
            Works Page
        </div>
    );
}

WorksPages.Layout = MainLayout