import { MainLayout } from '@/components/layout';
import * as React from 'react';

export interface BlogPagesProps {
}

export default function BlogPages(props: BlogPagesProps) {
    return (
        <div>
            Blog Page
        </div>
    );
}

BlogPages.Layout = MainLayout